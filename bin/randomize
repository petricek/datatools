#!/usr/bin/env bash

if [ "$SEED" != "" ]
then
        OPT="-v $SEED"
else
        OPT=""
fi

set -u
set -e

if which gsort 2> /dev/null >/dev/null
then
        sort=gsort
else
        sort=sort
fi        

$sort -k1,1 -R <(nl $OPT $*) | cut -f2- 
